extends ../../layout

block content
  
  .container.row.main
    
    include ../menu
    
    include _menu
    
    h1 Reports
    
    script
      window.barns = !{JSON.stringify(barns)};
      window.owls = !{JSON.stringify(owls)};
    
    .registration_graph